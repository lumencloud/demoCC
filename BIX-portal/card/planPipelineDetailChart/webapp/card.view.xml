<mvc:View
    controllerName="bix.card.planPipelineDetailChart.card"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
    width="100%"
    height="100%"
    displayBlock="true"
>
    <Panel
        expandable="false"
        expanded="true"
        width="auto"
        height="auto"
        id="cardContent"
    >        
        <content>
            <!-- <OverflowToolbar style="Clear" width="100%" height="2.5rem" class="sapUiMediumMarginBottom">
                <ToolbarSpacer />
                <SegmentedButton
                    id="detailSelect"
                    selectionChange=".onUiChange"
                    selectedKey="{uiModel>/key}"
                    class="custom-detail-select-btn"
                    items="{path: 'selectModel>/'}">
                        <items>
                            <SegmentedButtonItem key="{selectModel>sub_key}" text="{selectModel>sub_text}"/>
                        </items>
                </SegmentedButton>
            </OverflowToolbar> -->
            <VBox fitContainer="true">
                <FlexBox
                    alignItems="Center"
                    justifyContent="Center"
                    fitContainer="true"
                >
                    <core:HTML id="html0" />
                </FlexBox>
            </VBox>
        </content>
    </Panel>
</mvc:View>
